# This file runs on the NAS (~/eros/), not in the repo.
# All host paths are relative to that directory.
# deploy.sh copies this file, frontend builds, and Docker images to the NAS.

services:
  caddy:
    image: eros-caddy:latest
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    env_file: .env
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile
      - ./admin-build:/srv/admin       # SPA static files, copied by deploy.sh
      - ./client-build:/srv/client     # PWA static files, copied by deploy.sh
      - ./data/caddy:/data             # TLS certs (Let's Encrypt)
      - ./data/caddy-config:/config    # Caddy runtime config
    depends_on:
      backend:
        condition: service_healthy
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

  backend:
    image: eros-backend:latest
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./data/db:/data/db
      - ./data/files:/data/files
    environment:
      - DATABASE_PATH=/data/db/eros.sqlite
      - FILE_STORAGE_LOCAL_BASE_PATH=/data/files
      - SERVER_HOST=0.0.0.0
      - SERVER_PORT=8080
      - CORS_ALLOWED_ORIGINS=https://admin.eros.jacksonc.dev
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/api/health"]
      interval: 30s
      timeout: 5s
      retries: 3
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
