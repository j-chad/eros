meta {
  name: Delete Registration
  type: http
  seq: 3
}

delete {
  url: {{scheme}}{{host}}/api/admin/registration-codes
  body: none
  auth: inherit
}

script:post-response {
  bru.setEnvVar("registrationCode", "")
}

settings {
  encodeUrl: true
  timeout: 0
}
